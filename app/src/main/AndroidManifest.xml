<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufpe.cin.gossip">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gossip">
        <activity android:name=".NovaSala"></activity>
        <activity android:name=".Vizinhos" />
        <activity android:name=".SalasMenu" />
        <activity
            android:name=".MenuPerfil"
            android:label="@string/title_activity_m_enu"
            android:theme="@style/Theme.Gossip.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>